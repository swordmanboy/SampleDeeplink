# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DeeplinkTutorial' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'
  pod 'Firebase/DynamicLinks'
  # Pods for DeeplinkTutorial

  target 'DeeplinkTutorialTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DeeplinkTutorialUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
